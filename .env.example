# =============================================================================
# DOCKER CONFIGURATION
# =============================================================================

# Application port (Nginx)
APP_PORT=8080

# Database
DB_PORT=5432
DB_DATABASE=association
DB_USERNAME=association
DB_PASSWORD=secret

# Vite dev server
VITE_PORT=5173

# Mailpit
MAIL_PORT=8025
MAIL_SMTP_PORT=1025
